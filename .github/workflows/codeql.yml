name: CodeQL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # Run daily at 02:00 UTC
    - cron: '0 2 * * *'
  workflow_dispatch:

concurrency:
  group: codeql-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze (${{ matrix.language }} - ${{ matrix.build }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        language: [ javascript ]
        build: [ autobuild, none ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        if: ${{ matrix.build == 'autobuild' }}
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: '/language:${{ matrix.language }}'
          output: 'results/${{ matrix.language }}.sarif'
          upload: ${{ github.event_name != 'pull_request' }}

      - name: Save PR metadata
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          mkdir -p pr
          echo "${{ github.event.pull_request.number }}" > pr/pr_number.txt
          echo "${{ github.ref }}" > pr/ref.txt
          echo "${{ github.sha }}" > pr/sha.txt

      - name: Upload SARIF artifact
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions/upload-artifact@v4
        with:
          name: codeql-sarif-${{ matrix.language }}
          path: |
            results/${{ matrix.language }}.sarif
            pr/
          retention-days: 7

      - name: Upload SARIF to external dashboard
        if: ${{ (github.event_name == 'push' || github.event_name == 'schedule') && secrets.SARIF_UPLOAD_URL != '' && secrets.SARIF_UPLOAD_TOKEN != '' }}
        env:
          SARIF_UPLOAD_URL: ${{ secrets.SARIF_UPLOAD_URL }}
          SARIF_UPLOAD_TOKEN: ${{ secrets.SARIF_UPLOAD_TOKEN }}
        run: |
          curl -sSf -H "Authorization: Bearer $SARIF_UPLOAD_TOKEN" \
               -H "Content-Type: application/sarif+json" \
               --data-binary @results/${{ matrix.language }}.sarif \
               "$SARIF_UPLOAD_URL"
